# Sungwoo's Project — DESIGN.md

| 항목 | 내용 |
|---|---|
| 문서명 | Sungwoo's Project 디자인 가이드 |
| 버전 | v0.1 (초안) |
| 작성일 | 2026-08-13 |
| 연관 문서 | PRD.md |
| 상태 | 방향 확정 → 구현 착수 가능 |

---

## 1. 디자인 컨셉

### 1.1 한 문장 정의
> **공항 출발 안내판을 웹으로 옮긴 프로젝트 허브.**
> 겉은 안내판, 뼈대는 실무 문서. 화려함은 인트로 한 장면에만 몰아준다.

### 1.2 왜 이 컨셉인가

| 근거 | 설명 |
|---|---|
| 인터랙션에 명분이 있다 | 안내판은 원래 움직이는 물건이다. 플립, 상태 변경, 시간 표시가 컨셉에서 자연스럽게 나온다. 장식으로 얹은 애니메이션과 설계된 인터랙션의 차이가 여기서 갈린다. |
| 경력이 화면에 담긴다 | "여행업 출신 기획자"를 글로 설명하지 않고 화면이 말하게 한다. 면접 대화의 출발점이 된다. |
| 기획자의 언어다 | 프로젝트 상태(완료/진행중/준비중)가 안내판의 운항 상태로 치환된다. 정보를 어떻게 배열하고 상태를 어떻게 표현하는가 — 기획 역량이 드러나는 지점이다. |

### 1.3 지켜야 할 균형
안내판 컨셉은 잘못 다루면 촌스러워진다. **겉모습은 안내판, 규율은 실무 문서**로 간다.
- 격자는 정확히 맞춘다.
- 여백은 규칙적으로 (8px 배수).
- 정렬은 픽셀 단위로 신경 쓴다.
- 장식은 컨셉에서 나온 것만 남긴다.

---

## 2. 컬러

### 2.1 기본 팔레트

| 토큰 | HEX | 용도 |
|---|---|---|
| `--bg` | `#14161A` | 페이지 배경 (짙은 차콜) |
| `--panel` | `#1C2026` | 카드·모달 표면 |
| `--rule` | `#2C323A` | 괘선, 구분선, 테두리 |
| `--text` | `#F2F4F6` | 본문·제목 텍스트 |
| `--text-dim` | `#8A93A0` | 보조 텍스트, 라벨, 캡션 |
| `--accent` | `#2FB3C9` | 포인트색 — 에비에이션 시안 |

**배경을 순검정으로 하지 않은 이유** — 흰 글자를 `#000` 위에 올리면 대비가 과해 눈이 부시고, 상세 모달처럼 글이 많은 화면에서 피로가 빨리 온다. 차콜은 그 문제 없이 안내판 느낌을 유지한다.

### 2.2 상태 컬러

| 상태 | 안내판 표기 | HEX | 용도 |
|---|---|---|---|
| 완료 | `ARRIVED` | `#4FBF7B` | 완료된 프로젝트 |
| 진행중 | `BOARDING` | `#E8A33D` | 작업 중인 프로젝트 |
| 준비중 | `SCHEDULED` | `#8A93A0` | 아직 열 수 없는 카드 |

> 상태 표기를 운항 용어로 치환하는 것은 장식이 아니다. 세 상태가 실제로 **시간 순서**를 가지므로(예정 → 진행 → 완료) 안내판 어휘가 정보 구조와 일치한다.

### 2.3 컬러 사용 규칙
- 포인트색은 **선·글자·테두리·아이콘**에만 쓴다. 넓은 면적을 채우지 않는다.
- 한 화면에서 포인트색이 쓰이는 지점은 **3곳 이하**로 유지한다.
- 상태 컬러는 배지와 상태등에만 쓰고, 텍스트 본문에는 쓰지 않는다.
- 그라데이션은 쓰지 않는다. 안내판은 단색 발광이다.

---

## 3. 타이포그래피

### 3.1 폰트 구성 (2종)

| 역할 | 폰트 | 대상 |
|---|---|---|
| 데이터·디스플레이 | **JetBrains Mono** | 프로젝트명, 상태 배지, 시각, 숫자, 라벨, 태그 |
| 본문 | **Pretendard** | 모달 안쪽 문단, 설명문, 한글 전반 |

- 둘 다 무료이며 웹폰트로 바로 쓸 수 있다.
- 고정폭을 사이트 전체에 도배하지 않는다. **읽는 화면(모달 본문)은 반드시 Pretendard.**
- 한글이 고정폭으로 렌더링되지 않도록 font-family 순서를 관리한다.

### 3.2 타입 스케일 (기준 16px · 중간 밸런스)

| 토큰 | 크기 | 폰트 | 자간 | 용도 |
|---|---|---|---|---|
| `display` | 40 → 72px (반응형) | Mono 700 | +0.08em | 인트로 타이틀 |
| `h1` | 28px | Mono 600 | +0.04em | 페이지 제목 |
| `h2` | 20px | Mono 600 | +0.02em | 카드 프로젝트명, 모달 섹션 제목 |
| `body` | 16px / 행간 1.7 | Pretendard 400 | 0 | 본문 문단 |
| `label` | 13px | Mono 500 | +0.12em, 대문자 | 라벨, 상태, 태그 |
| `micro` | 11px | Mono 400 | +0.1em | 시각, 각주 |

### 3.3 타이포 규칙
- 라벨과 상태는 **영문 대문자 + 넓은 자간**. 안내판의 핵심 질감이 여기서 나온다.
- 본문 한 줄은 **최대 70자**를 넘기지 않는다 (모달 내부 폭 제한).
- 본문에는 자간을 넣지 않는다. 한글은 자간을 벌리면 오히려 읽기 어려워진다.
- 굵기는 400 / 500 / 600 / 700 네 단계만 쓴다.

---

## 4. 레이아웃

### 4.1 기본 규격

| 항목 | 값 |
|---|---|
| 최대 폭 | 1120px |
| 간격 단위 | 8px 배수 (4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96) |
| 모서리 반경 | 2px — 기계식 패널이므로 거의 각지게 |
| 테두리 | 1px `--rule` |
| 그림자 | 쓰지 않음 (모달 배경 오버레이 제외) |

### 4.2 화면 구조

**[P1] 인트로**
```
┌──────────────────────────────────────────────┐
│                                    SEL 14:32 │  ← 실시간 시계 (서울)
│                                              │
│   ▚▚▚  S U N G W O O ' S   P R O J E C T ▚▚  │  ← 스플릿 플랩 타이틀
│   ──────────────────────────────────────     │
│   여행업 4년, 문제를 시스템으로 옮기는 기획자    │  ← 한 줄 소개
│                                              │
│   HANATOUR · 동남아상품팀(태국) MD · 4년       │  ← 경력 요약
│   원가관리 / 신규 상품 기획                     │
│                                              │
│   ▸ ENTER                                    │  ← 포인트색 CTA
│                                              │
│   sungwoo@email.com   GITHUB   LINKEDIN      │  ← 연락처
└──────────────────────────────────────────────┘
```

**[P2] 프로젝트 리스트 — 안내판 행 구조**
```
┌──────────────────────────────────────────────────────────┐
│  SUNGWOO'S PROJECT                    CAPABILITIES  14:32│
├──────────────────────────────────────────────────────────┤
│  NO   PROJECT           SUMMARY              STATUS      │  ← 헤더 행
├──────────────────────────────────────────────────────────┤
│  01   COSTPILOT         원가표 표준화·자동화   ● BOARDING  │
│  02   PORTFOLIO         실무 케이스 스터디     ○ SCHEDULED │
│  03   SUNGWOO'S PROJECT AI로 만든 이 허브      ● ARRIVED   │
└──────────────────────────────────────────────────────────┘
```

**[P3] 상세 모달**
```
┌────────────────────────────────────────┐
│  01  COSTPILOT                     ✕   │
│  ────────────────────────────────────  │
│  문제 정의                              │  ← 여기부터 Pretendard
│  본문 …                                 │
│  내 역할 · 기여도                        │
│  진행 과정 · 의사결정                     │
│  결과 · 성과                            │
│  ────────────────────────────────────  │
│  STACK  HTML  JS  SHEETJS  CLAUDE      │  ← 다시 Mono
│  [스크린샷]                             │
│  ▸ GITHUB   ▸ 기획 문서                 │
└────────────────────────────────────────┘
```

### 4.3 리스트 구조 — 행 기반 (확정)
프로젝트는 그리드가 아닌 **행 리스트**로 배열한다. 실제 안내판은 항공편을 행으로 나열하고, 프로젝트가 3개뿐이라 그리드로 배치하면 허전하다. 행 구조는 3개여도 자연스럽고 훑는 속도도 빠르다.

- 열 구조: `NO / PROJECT / SUMMARY / STATUS`
- 태그·썸네일은 행에서 빼고 상세 모달로 옮긴다. 행에 이미지가 들어가면 안내판 구조가 무너진다.
- 프로젝트가 6개를 넘으면 그리드 전환 또는 정렬·필터 도입을 재검토한다.

→ PRD FR-2.1 ~ FR-2.7에 반영 완료.

### 4.4 반응형

| 구간 | 처리 |
|---|---|
| ≥ 1024px | 안내판 행 구조 그대로 |
| 768 ~ 1023px | 요약 열 축약, 상태는 유지 |
| < 768px | 행 → 카드형으로 전환. 프로젝트명 + 상태 + 요약 3줄 |

모바일에서는 스플릿 플랩 글자 수를 줄이거나 2줄로 나눈다. 좁은 화면에서 글자가 뭉개지면 컨셉이 아니라 사고로 보인다.

---

## 5. 인터랙션

### 5.1 원칙
> **화려함은 한 곳에만.**
> 인터랙션이 화면마다 흩어져 있으면 "라이브러리 붙였구나"로 읽히고, 한 장면에 응축돼 있으면 "설계했구나"로 읽힌다.

보조 인터랙션은 **눈에 띄면 실패**다. 있는지도 모를 정도로 자연스러워야 한다.

### 5.2 시그니처 장면 (하나의 연속 동작)

**① 스플릿 플랩 인트로**
- 페이지 진입 시 타이틀 글자가 플랩처럼 돌아가며 `SUNGWOO'S PROJECT`로 맞춰진다.
- 글자당 60ms 간격으로 순차 진행. **전체 1.4초 이내 완료.**
- 애니메이션 도중에도 ENTER는 즉시 클릭 가능하다.

**② 보딩 트랜지션**
- ENTER 클릭 시 인트로 정보가 플랩이 넘어가듯 위로 접히고, 그 자리에 안내판 행이 아래에서 드러난다.
- 전환 시간 400ms. ①과 ②는 **끊기지 않는 한 장면**으로 설계한다.

### 5.3 보조 인터랙션

| 항목 | 사양 |
|---|---|
| 카드 호버 | 행 배경이 `--panel`로 밝아지고 좌측에 시안 1px 세로선. 전환 80ms. |
| 실시간 시계 | 우측 상단에 서울 시각(HH:MM), 1분 단위 갱신. Mono, `micro` 크기, `--text-dim`. |
| 상태등 점멸 | **'진행중(BOARDING)' 카드만.** 3초 주기의 느린 밝기 변화(0.4 → 1.0). 깜빡임(blink) 아님. |
| 키보드 조작 | ↑↓ 행 이동 / Enter 상세 열기 / Esc 닫기. 포커스 위치는 시안 테두리로 명시. |

> **상태등에 조건을 단 이유** — 진짜 깜빡이면 시선을 계속 뺏겨 카드 읽기를 방해하고, 광과민성 접근성 문제도 생긴다. 느린 밝기 변화로 대체한다.

### 5.4 모션 규칙

| 상황 | 시간 | 이징 |
|---|---|---|
| 호버·포커스 | 80ms | ease-out |
| 모달 열기·닫기 | 200ms | cubic-bezier(0.2, 0, 0, 1) |
| 플랩 1글자 | 120ms | steps 느낌의 빠른 감속 |
| 보딩 트랜지션 | 400ms | cubic-bezier(0.2, 0, 0, 1) |

**`prefers-reduced-motion` 대응** — 이 설정이 켜진 사용자에게는 플랩·트랜지션·상태등 점멸을 모두 끄고 최종 상태를 즉시 표시한다. 선택이 아니라 필수다.

---

## 6. 금지사항

### 6.1 확정 금지 (본인 결정)
1. **3D · 패럴랙스 스크롤 금지** — 카드 3개짜리 사이트에 과하다.
2. **스톡 일러스트 · 이모지 아이콘 금지** — 컨셉과 무관하고 완성도를 떨어뜨린다.
3. **인트로 애니메이션 2초 초과 금지, 스킵 불가 금지** — 채용 담당자는 로딩을 기다려주지 않는다.
4. **자동 재생 사운드 금지.**

### 6.2 권장 금지 (미확정 · 검토용)
아래는 제안 단계이며 아직 확정되지 않았다. 작업하면서 채택 여부를 결정한다.

- 커서 추적 효과, 파티클, 배경 그라데이션 애니메이션
- 모달 안쪽 장식 — 읽는 화면이므로 조용해야 한다
- 검증 불가한 수치 표기 (PRD §9 참조)
- 포인트색을 넓은 면적에 채우기
- 스크롤 연동 애니메이션 — 스크롤할 분량 자체가 없어 억지가 된다

---

## 7. 카피 톤

- **라벨은 안내판 어휘, 본문은 평서문.** 상태·태그·헤더는 영문 대문자 운항 용어, 설명은 한국어 문장.
- **버튼은 결과를 말한다.** "제출"이 아니라 "ENTER", "닫기"처럼 눌렀을 때 벌어지는 일을 쓴다.
- **빈 상태도 안내판처럼.** 준비중 카드는 "준비 중입니다" 대신 `SCHEDULED — 준비 중` 형식으로 상태를 먼저 보여준다.
- **과장 금지.** "혁신적인", "최적화된" 같은 수식어를 쓰지 않는다. 무엇을 했는지만 쓴다.

---

## 8. 품질 기준 (구현 시 확인)

- [ ] 인트로 첫 화면 렌더 2초 이내, 플랩 완료 1.4초 이내
- [ ] 애니메이션 중에도 ENTER 클릭 가능
- [ ] `prefers-reduced-motion` 켜면 모든 모션 정지
- [ ] 키보드만으로 전체 탐색 가능, 포커스가 항상 눈에 보임
- [ ] 모달 열림 시 포커스가 모달 안으로 이동, 닫으면 원래 위치 복귀
- [ ] 텍스트 대비 4.5:1 이상 (`--text-dim`은 큰 글자에만)
- [ ] 375px 폭에서 레이아웃 깨짐 없음
- [ ] 이미지 alt 텍스트 작성

---

## 9. 미결정 사항

| # | 항목 | 비고 |
|---|---|---|
| 1 | 인트로 한 줄 소개 확정 문구 | PRD §9와 동일 항목 |
| 2 | 스크린샷 실물 확보 | CostPilot MVP 화면 |
| 3 | 파비콘·OG 이미지 | 링크 공유 시 노출 |
| 4 | 추가 금지사항 (§6.2 채택 여부) | 작업하며 결정 |

---

## 10. 참고 — 방콕 시계
서울/방콕 이중 시계 안은 이번 범위에서 제외한다. 필요 시 우측 상단 시계 블록에 행을 추가하는 형태로 확장 가능하도록 구조만 열어둔다.
